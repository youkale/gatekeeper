# T-20260718-02 M2 本地 CLI

- **规格**：docs/designs/M2-cli.md（退出码契约经 T-01 R1 review 修订）
- **编码**：sonnet-coder（后台，首轮交付 41/41 绿，输出含 4 条自报风险）

## Review 第 1 轮（并行，双 FAIL）

- **claude-reviewer（opus）：FAIL**，2 blocker：
  1. EPIPE 无守卫——`check --json | true` 实测 5/5 次 exit 1，pass/warn/degraded 被断管翻成非 0（fail-open 违反，CI 常见管道消费者即可触发）。
  2. `parseNameStatusZ` 对 T/U/X 静默丢弃——守护文件 `ln -s` 成 symlink（git 报 T）即绕过关口静默 pass（漏判定/关口绕过）。
  - 退出码矩阵其余各格实测正确；引擎禁改区无改动；自报风险 a/c/d 判非缺陷。
- **codex-reviewer：FAIL**（wrapper 代理因模型额度错误中途死亡，调度者直接收割 codex job review-mrp97rps-1ovs9d，session 019f7146-6632-7b53-8aae-3d95021e15aa），2 P1 + 4 P2：
  - P1 rename patch 不带 -M 双路径 → 纯改名全 + 行 → if_content 误命中误阻塞（**与 claude 非 blocker 结论冲突，仲裁采纳 codex**：误阻塞噪音是信任杀手）；P1 单文件 diff 失败返回 undefined 被引擎 fail-open 视为命中 → 基建故障制造 block。
  - P2：缺 working-tree 模式（规格承诺）；--json --strict-infra 不出 degraded JSON；patch 预筛误含 exclude（性能）；fsregistry readdir 未排序（输出不确定性）。

## 仲裁

8 项全部采纳（blocker 4 + P2 4），一轮合并派回原编码 agent（sonnet-coder 续场）。修法要点：EPIPE 吞到 exit 0；未知 status 归一 M + stderr 提示（fail-closed）；R/C 双路径 -M 拉 patch；命令失败抛 GitDiffError 走 degrade、仅 binary 合法 undefined。

## Review 第 2 轮（增量，双 PASS）

- **codex-reviewer：PASS**（thread 019f72e8-a3f4-7a73-9879-0d148ccd940e，修订后前台 --wait 规程首跑正常）。8/8 修复确认，无新 blocker；其沙箱无法跑全量 vitest，主环境补跑。
- **claude-reviewer（opus，续场督促一次后正常执行）：PASS**。8/8 亲测确认 + 4 项对抗核验全过（EPIPE 不吞非 EPIPE、block 已定 exit 1 在 EPIPE 下保留、R 语义不受归一影响、空串/undefined patch 语义边界正确）。补测了互斥第三组合。

## 验收（调度者，2026-07-18）

- `npm run typecheck` ✅ `npm test` 49/49 ✅ `npx biome check src tests` ✅
- **终态：2 轮闭环（R1 双 FAIL 8 项 → R2 双 PASS），验收通过并提交。**
- 遗留（M8 文档义务）：--working-tree 对 untracked 文件不可见需文档注明；resolveBaseRef 仅探本地 main/master 的 CI 语境提示。
