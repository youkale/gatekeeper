# T-20260719-02 agent 绑定纠偏 B：MCP server

- **交付**：integrations/mcp/（gatekeeper_check / gatekeeper_validate / gatekeeper_brief 三工具，stdio，复用主包引擎零复制、零模型）+ 真实协议往返测试 + 各 client 接入文档；编码期实测发现并规避 MCP SDK 对 ZodEffects schema 的展示缺陷。
- **R1（双 FAIL 同一并发缺陷）**：captureStreams 全局劫持 stdout 与 SDK 响应写出同流、SDK 不串行化请求——grok 从 SDK 源码三处取证（包装层剔除其幻觉的 probe 文件佐证后独立重验，验证纪律范本）；claude 活体复现协议流损坏（并发 B 的 JSON-RPC 帧被吞进 A 文本、B 超时）。另：claude 裁定「援引 action.ts captureCommand 先例」不成立——单发进程安全 ≠ 常驻并发 server 安全。
- **仲裁**：根除方案（否决加锁治标）——runValidate 注入式 sink（可选参，CLI 零变化），captureStreams 全删；双前缀顺修；回归测试经 mutation 自证有效。
- **R2**：claude PASS——真实 stdio 子进程 12 路并发全部独立正确、帧零泄漏、双前缀消失、e2e 27/27 无回归。
- **验收**：typecheck / 350测试 / biome / check:governance 全绿。终态：验收提交。
