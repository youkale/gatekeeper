# T-20260719-03 交互简化 A：配置发现 + adopt/provision

- **形态（三次用户中途修正后定稿）**：`.gatekeeper.yml` 向上发现（CLI>env>配置>缺省，check/gate 坏损 fail-open、其余 fail-loud）；`adopt --control <总控仓> [被控仓=cwd]` 只登记（目标仓最小配置 + 注册表侧 repos.yaml，realpath 重叠校验四态拒绝）；`provision [repos...]` 总控仓批量落 CI（gitlab marker 注入/github 模板）/pre-push hook（fail-open）/AGENTS.md 块，幂等 + dry-run + 单仓失效跳过。
- **R1**：grok PASS（6 nb，采纳 realpath 统一与坏配置警告 2 项）；claude FAIL 1 blocker——git worktree（.git 为文件）ENOTDIR 未捕获、整批 provision 崩溃中断（活体复现，击穿批量韧性自有不变量）。
- **修复**：git rev-parse --git-common-dir 正解 worktree hooks 目录 + runProvision 循环级 try/catch 纵深防御；修前 dist 复现崩溃/修后同场景通过的前后对照。
- **R2**：claude PASS——worktree 场景独立脚本复现修复生效、注入式 TypeError probe 证批次防御隔离且 exit 1 可感知（定性：编排命令的错误隔离，非判定路径，合规）。
- **验收**：typecheck / 409 测试 / biome / build / check:governance 全绿。终态：验收提交。
