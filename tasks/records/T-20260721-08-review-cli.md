# T-20260721-08 review E 包：CLI 命令族 + review-ledger

## 交付（sonnet-coder，2 轮）

- src/commands/review.ts + cli.ts：九子命令（start/status/logs/fix/accept/arbitrate/resume/cancel/render）；driveCycle 状态驱动委托（落实 C 包交接①）；lane 组路（cross-vendor + authoring 排除 + --allow-degraded）；--waive <id>=<reason> 编码；render 独立版本化 marker（与 gate sticky marker 互斥测试锚定）；review-ledger 行；退出码 0/2/3 永不 1；releaseLockSafely（命令层持锁 finally 吞错告警）。
- 测试 45 条 + e2e 冒烟。

## 闭环（降级验收）

- grok 仍掉登录 → 对抗 claude(opus) 替补第二路，补审债与 C 包同批。
- R1：主路 claude(opus) FAIL 1 blocker（探针实锤：accept/arbitrate/cancel 裸 finally release 抛错穿透 exit 1——与 dispatch E 包 cancel 同族第 2 例）；替补路 PASS。
- R2：主路 PASS（探针重放四路径 0/0/3/3、releaseLockSafely 健壮性、R1x 旧漏洞实锤修复核实）。

## 规范修订（同类 ≥2 触发，入 LESSONS）

CLI 命令实现任何路径不得让 promise reject；命令层持锁的 finally release 必须吞错告警——今后 CLI 类任务包固定条款。

## 验收（调度者，2026-07-21，降级标注）

typecheck ✅ 1053/1053 ✅ biome ✅ build ✅ governance ✅ 字节零 → 验收提交。
