# T-20260719-04 交互简化 B：BYO agent runner

- **交付**：.gatekeeper.yml 可选 agent 段（command 无默认必显式/timeout≤3600）；src/agent/runner.ts 双模式（{brief}/{out} 占位符 shell 执行 / stdin-stdout 管道）+ SIGTERM→SIGKILL；triage --run（简报→agent→复用全套硬校验→TTY 确认或 --yes→复用 post 链路）；init --run（agent 起草自洽草稿目录→validate --strict 收口）。产品保持零模型（仅 spawn 用户声明命令，信任边界同 git hooks）。
- **R1 三 blocker 各有出处**：claude——占位符裸拼 shell，空格/单引号路径双场景活体复现（词分裂/引号态翻转）；grok——init --run 校验失败误映射 exit 1（测试还固化了错误语义）、shell:true 超时只杀 shell 不杀孙进程（README 主推形态正中此坑）。另采纳 stdin EPIPE 硬化与 grok 示例统一 --prompt-file。
- **修复**：shellQuote（仅我方路径值）；detached + 负 pid 组信号（Windows 回退注释）；exit 2 对齐；三条回归全部做了修前/修后 mutation 对照（含三层进程树孙进程 SIGKILL 实测）。
- **R2**：claude PASS（用户命令自带引号不被误改探针、正常路径无 detached 副作用、5s 悬挂窗口定性可接受→.unref() 记后续建议）；grok PASS（时序/ESRCH/pid 复用注释如实性核验）。
- **验收**：typecheck / 435 测试 / biome / build / check:governance 全绿。终态：验收提交。
